interface reviewTypes {
  id: string;
  heading: string;
  desc: string;
  avatar: string;
}

interface servicesDetailsCardTypes {
  id: number;

  heading: string;
  detail: string;
}

export { reviewTypes, servicesDetailsCardTypes };
